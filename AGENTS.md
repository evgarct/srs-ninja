# Repository Instructions

- When creating or updating a GitLab merge request, always fill in the `Add a description` field.
- Do not leave GitLab's `Add a description` field empty.
- Before every push, update `docs/` and `specs/` to match the code changes in the branch.
